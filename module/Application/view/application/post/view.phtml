<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">
            <strong><?php echo $post->getTitle(); ?></strong>
            <span class="pull-right">par <strong><?php echo $post->getAuthor()->getUsername(); ?></strong></span>
        </h3>
    </div>
    <div class="panel-body">
        <p><?php echo nl2br($post->getContent()); ?>
        <?php
        if($post->getDateCreate() != $post->getDateEdit())
        {
            ?>
            <p><em>Dernière modification le <?php echo $post->getDateEdit()->format('d\/m\/Y\ \à\ H\:i'); ?></em></p>
            <?php
        }

        if($this->zfcUserIdentity())
        {
            ?>
            <p>
                <?php
                if($rating) {
                    echo $rating->getValue().'/5';
                }
                else {
                    echo 'Actuellement non noté';
                }
                ?>
                <span class="pull-left">
                    <a href="<?php echo $this->url('post-rate', array('pid' => $post->getId(), 'rating' => 1)); ?>" title="Noter le billet 1/5"><span class="glyphicon glyphicon-star"></span></a>
                    <a href="<?php echo $this->url('post-rate', array('pid' => $post->getId(), 'rating' => 2)); ?>" title="Noter le billet 2/5"><span class="glyphicon glyphicon-star"></span></a>
                    <a href="<?php echo $this->url('post-rate', array('pid' => $post->getId(), 'rating' => 3)); ?>" title="Noter le billet 3/5"><span class="glyphicon glyphicon-star"></span></a>
                    <a href="<?php echo $this->url('post-rate', array('pid' => $post->getId(), 'rating' => 4)); ?>" title="Noter le billet 4/5"><span class="glyphicon glyphicon-star"></span></a>
                    <a href="<?php echo $this->url('post-rate', array('pid' => $post->getId(), 'rating' => 5)); ?>" title="Noter le billet 5/5"><span class="glyphicon glyphicon-star"></span></a>
                </span>
                <?php
                if ($post->getAuthor()->getId() == $userme->getId() || $userme->getRoles()[0]->getRoleId() == "administrator")
                {
                    echo '<a class="btn glyphicon glyphicon-remove pull-right" title="Supprimer" href="'.$this->url('post-crud', array('action' => 'delete','pid' => $post->getId())).'"></a>';
                    echo '<a class="btn glyphicon glyphicon-pencil pull-right" title="Éditer" href="'.$this->url('post-crud', array('action' => 'update','pid' => $post->getId())).'"></a>';
                }
                ?>
            </p>
            <?php
        }
        ?>
    </div>
    <div class="panel-footer">
        <strong>Tags&nbsp;:</strong>
        <?php
        foreach($tags as $t)
        {
            echo '<a href="'.$this->url('search', array('tag' => $t)).'">'.$t.'</a> ';
        }
        ?>
    </div>
    <div class="panel-footer">
        <p class="panel-title">
            <span class="zf-green">Posté le <?php echo $post->getDateCreate()->format('d\/m\/Y\ \à\ H\:i'); ?></span>
            <img class="pull-right" src="<?php echo $this->basePath('img/zf2-logo.png'); ?>" alt="ZF2" />
        </p>
    </div>
</div>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">Commentaires (<?php echo $nb_comment; ?>)</h3>
    </div>
    <div class="panel-body">
        <table class="table table-bordered">
        <?php 
        if($nb_comment == 0)
        { 
            ?>
                <tbody><tr><td>Pas de commentaires</td></tr></tbody>
            </table>
            <?php
        }
        else
        {
            ?>
            <thead>
                <tr style="background-color: #68b604 ; color: white">
                    <th style="width: 120px">Pseudo</th>
                    <th>Commentaire</th>
                </tr>
            </thead>
            <tbody>
                <?php
                foreach($this->comment as $comm)
                {
                    ?>
                    <tr>
                        <td class="text-center"><?php echo $this->gravatar($comm['author']['email'])->setImgSize(100); ?></td>
                        <td>
                            <?php
                            echo nl2br($comm['content']);
                            if($comm['date_create'] != $comm['date_edit'])
                            {
                                ?>
                                <p><em>Dernière modification le <?php echo $comm['date_edit']->format('d\/m\/Y\ \à\ H\:i'); ?></em></p>
                                <?php
                            }
                            if ($comm['author']['id'] == $userme->getId() || $userme->getRoles()[0]->getRoleId() == "administrator")
                            {
                                echo '<p><a class="btn glyphicon glyphicon-remove pull-right" title="Supprimer" href="'.$this->url('comm-ud', array('action' => 'delete','cid' => $comm['id'])).'"></a>';
                                echo '<a class="btn glyphicon glyphicon-pencil pull-right" title="Éditer" href="'.$this->url('comm-ud', array('action' => 'update','cid' => $comm['id'])).'"></a></p>';
                            }
                            ?>
                        </td>
                    </tr>
                    <tr>
                        <td class="text-center" style="border-top:#FFFFFF;"><strong><?php echo $comm['author']['username']; ?></strong></td>
                        <td><span class="zf-green">Posté le <?php echo $comm['date_create']->format('d\/m\/Y\ \à\ H\:i'); ?></span></td>
                    </tr>
                    <?php
                } //foreach
                ?>
                </tbody>
            </table>
            <?php 
            if($nb_comment > 5)
            { 
                ?>
                <div class="text-center">
                    <ul class="pagination">
                        <?php
                        // Page précédente
                        if($this->page == 1) {
                            echo '<li class="disabled"><a href="#">&laquo;</a></li>';
                        }
                        else {
                            echo '<li><a href="'.$this->url('post-view', array('page' => ($this->page - 1))).'">&laquo;</a></li>';
                        }

                        // Liste des pages
                        for($i = 1; $i <= $nb_pages; ++$i) {
                            echo '<li'.(($i == $this->page) ? ' class="active"' : '').'>'
                               . '<a href="'.$this->url('post-view', array('page' => $i)).'">'.$i.'</a>'
                               . '</li>';
                        }

                        // Page suivante
                        if($this->page == $this->nb_pages) {
                            echo '<li class="disabled"><a href="#">&raquo;</a></li>';
                        }
                        else {
                            echo '<li><a href="'.$this->url('post-view', array('page' => ($this->page + 1))).'">&raquo;</a></li>';
                        }
                        ?>
                    </ul>
                </div>
                <?php
            }
        } // else
        ?>
    </div>
    <?php
    if($this->zfcUserIdentity())
    {
        ?>
        <div class="panel-footer">
            <p class="text-center" style="margin: 0px;"><a class="btn btn-success" style="margin: 0px;" href="<?php echo $this->url('comm-create', array('pid' => $post->getId())); ?>">Ajouter un commentaire</a></p>
        </div>
        <?php
    }
    ?>
</div>